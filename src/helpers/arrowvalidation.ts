export type ArrowvalidationType = {
    dayFromDate : number
     monthFromDate : string
    yearFromDate : number
    hourFromDate : number
    minutesFromDate : number
    secondsFromDate : number

}


/*export const arrowValidator<ArrowvalidationType> =(dayFromDate, monthFromDate, yearFromDate, hourFromDate, minutesFromDate, secondsFromDate):ArrowvalidationType=> {
    if (dayFromDate > 15) {
        parser(`${22} ${monthFromDate}`)
    }

}*/
export const x= 0

